initSidebarItems({"enum":[["Configuration",""]],"fn":[["initialize","Initializes an `allocator::Generic` for each thread, spawns the local threads, and invokes the supplied function with the allocator. Returns only once all threads have returned."]]});